const protocol = window.location.protocol
const opapiHost = `${protocol}//detikpinservicefe-api.detikpinservicefe.com`
let LoginUrl = `${protocol}/login` // 登录页

export default{
    opapiHost,
    LoginUrl
}